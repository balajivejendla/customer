# Docker Environment Variables
# Use this when running with Docker Compose

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-make-it-very-long-and-random
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-make-it-different-from-jwt-secret

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:8080

# Server Configuration
PORT=4000
SOCKET_PORT=3001

# Security
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=5

# Redis Configuration (Docker)
REDIS_ENABLED=true
REDIS_CLOUD_HOST=redis
REDIS_CLOUD_PORT=6379
REDIS_CLOUD_USERNAME=
REDIS_CLOUD_PASSWORD=
REDIS_DB=0

# Cache TTL (Time To Live) in seconds
CACHE_MESSAGE_TTL=3600
CACHE_RESPONSE_TTL=86400
CACHE_SESSION_TTL=900
CACHE_USER_TTL=1800

# MongoDB Atlas Configuration (REQUIRED)
MONGODB_URI=mongodb+srv://ecommerce_faq:Balaji90@chatbot.cvd1i2p.mongodb.net/ecommerce_faq?appName=Chatbot
MONGODB_DB_NAME=ecommerce_faq
FAQ_COLLECTION_NAME=faq_knowledge_base

# Google Gemini Configuration (REQUIRED - for both embeddings and chat)
GOOGLE_API_KEY=AIzaSyCADdcipExIK1qDr2OxqlYQRez71tmBo4A
GEMINI_MODEL=gemini-1.5-pro

# Google Embedding Configuration (uses same API key)
GOOGLE_EMBEDDING_MODEL=text-embedding-004
GOOGLE_EMBEDDING_DIMENSIONS=768

# RAG Configuration (updated for Google embeddings - 768 dimensions)
VECTOR_SEARCH_INDEX_NAME=vector_search_index
SIMILARITY_THRESHOLD_HIGH=0.85
SIMILARITY_THRESHOLD_LOW=0.75
MAX_RETRIEVAL_RESULTS=3
MAX_CONTEXT_LENGTH=4000

# Environment
NODE_ENV=development